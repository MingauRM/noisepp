[package]
name = "mingaurm/noisepp"
version = "1.0.1"
description = "A noise module that adds noise types and features that the math.noise function doesn't have."
repository = "https://github.com/MingauRM/noisepp"
license = "None"
authors = ["Miguel Rossetti Mocellin"]
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
exclude = ["rokit.toml", "selene.toml", "README.md"]

[dependencies]
